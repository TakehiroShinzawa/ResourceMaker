<Window x:Class="ResourceMaker.UI.ResourceEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ResourceMaker.UI"
        mc:Ignorable="d"
        Title="ResourceEditWindow" Height="450" Width="800">
    <Grid>
        <StackPanel Margin="20">
            <StackPanel Orientation="Vertical">
                <TextBlock x:Name="LabelTargetText" Text="" FontSize="14"/>
                <TextBox x:Name="SelectedText" FontSize="14"
                    IsReadOnly="True" 
                    Margin="0,0,5,0"
                    VerticalScrollBarVisibility="Auto"/>
            
                <TextBlock x:Name="LabelUpdatedText" Text="書き戻しテキスト" FontSize="14"/>
                <TextBox x:Name="ResultText" FontSize="14"
                    IsReadOnly="false" 
                    Margin="0,0,5,0"
                    VerticalScrollBarVisibility="Auto"/>


            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <TextBlock x:Name="OriginalText" Text="" VerticalAlignment="Center" Margin="0,5,0,5"/>
                <ComboBox Name="BaseTexts" VerticalAlignment="Center" Margin="0,5,0,5" Width="200"
                    SelectionChanged="BaseTexts_SelectionChanged" />
            </StackPanel>
            <StackPanel Orientation="Horizontal" Focusable="False">
                <TextBlock x:Name="ResourceKey" Text="リソース名を入力" VerticalAlignment="Center" Margin="0,5,0,5"/>
                <TextBox x:Name="ResourceKeyBox"
                    Width="200"
                    Margin="5"
                    VerticalAlignment="Center"
                    Padding="0,0,25,0"
                    GotFocus="TextBox_GotFocus"
                    LostFocus="ResourceKeyBox_LostFocus"
                    KeyDown="TextBox_KeyDown" />

                <Button x:Name="ClearButton"
                    Content="🗑"
                    Width="30"
                    Height="25"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Center"
                    Margin="0,0,15,0"
                    Focusable="False"
                    Click="ResourceClearButton_Click"/>
                <Button x:Name="ResourceGetterExpression"
                    Content="取得式"
                    VerticalAlignment="Center"
                    Margin="0,5,0,5"
                    Click="ResourceGetterExpression_Click"/>
                
                <TextBox x:Name="ResourceGetterBox"
                    Width="150"
                    Margin="5"
                    IsTabStop="False"
                    GotFocus="TextBox_GotFocus"
                    LostFocus="ResourceGetterBox_LostFocus"
                    VerticalAlignment="Center"
                    Padding="0,0,25,0"
                    ToolTipService.ToolTip=""
                    KeyDown="TextBox_KeyDown" />
                <CheckBox Name="Feedback" Content="" 
                    IsChecked="True" 
                    Focusable="False"
                    VerticalAlignment="Center" />

            </StackPanel>
            <ItemsControl ItemsSource="{Binding LanguageEntries}" Focusable="False">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal" Margin="0,5,0,5" >
                            <TextBlock Text="{Binding Code }"
                                VerticalAlignment="Center"
                                Width="50"
                                Margin="0,0,0,0" />

                            <TextBox Width="250"
                                Margin="5,0,0,0"
                                VerticalAlignment="Center"
                                Padding="0"
                                AcceptsReturn="True"
                                TextWrapping="Wrap"
                                VerticalScrollBarVisibility="Auto"
                                Text="{Binding Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                            <Button Content="🗑"
                                Margin="5,0,0,0"
                                Width="30"
                                Height="25"
                                Focusable="False"
                                Command="{Binding DataContext.ClearCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                CommandParameter="{Binding}" />
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
            <StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <Button x:Name="SaveResource" Content="リソースの保存" Click="SaveResource_Click" Margin="5,0,0,0"/>
                    <Button x:Name="SaveAndClose" Content="反映" Click="SaveResource_Click" Margin="15,0,0,0"/>
                    <Button x:Name="AddLanguage" Content="言語の追加" Click="AddResource" Margin="25,0,0,0"/>
                    <Button x:Name="ClearCache" Content="言語のクリア" Click="ClearCache_Click"  Margin="25,0,0,0"/>
                </StackPanel>
            </StackPanel>
            <StackPanel HorizontalAlignment="Center">
                <TextBox x:Name="ClipText"/>
                <TextBlock x:Name="Infomation" Text="" TextWrapping="Wrap" />
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
